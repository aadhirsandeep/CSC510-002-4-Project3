[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fastapi_app"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
fastapi-app = "app.__main__:main"
